experiment:
  name: 
  seed: 
  notes:

model:
  architecture: 
  encoder:
    hidden_dim: 
    num_layers: 
    num_heads: 
  decoder:
    num_layers: 
    num_queries: 
  dropout: 

training:
  optimizer: 
  lr: 
  weight_decay: 
  batch_size: 
  num_epochs: 
  scheduler: 
  warmup_steps: 
  grad_clip: 

logging:
  log_every_n_steps: 
  checkpoint_every_n_epochs: 
  